# Production build-time environment.
# Vite inlines VITE_* vars into the bundle at build time.
#
# AWS deploy (branch deploy/aws): frontend and backend share one CloudFront
# distribution — /api/* is proxied to the ALB. Same origin, so a relative
# base works and there is no CORS.
VITE_API_BASE=/api
